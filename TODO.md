

Check what happens when trying to init a project without a tool install IE. Cargo - DONE. Prints error stating tool is not added to path. Also added a small message stating to check the output.

Add git and docker commands to command dictionary. Flesh out bash and powershell commands more.

Add a "tool check" function to see if a tool is installed and return a boolean
